# --- PyTorch (古い ONNX exporter を使うために必須) ---
torch==2.0.1
torchvision==0.15.2

# --- NumPy (PyTorch 2.0.1 と ONNXRuntime 1.16.3 の必須条件) ---
numpy==1.26.4

# --- Diffusers / Transformers (SD1.5 読み込み用) ---
diffusers==0.20.2
transformers==4.30.2
accelerate==0.21.0
safetensors==0.3.3

# --- ONNX Export (安定版) ---
onnx==1.14.1
onnxruntime==1.16.3

# --- Hugging Face Hub ---
huggingface_hub>=0.23.0
hf_transfer>=0.1.6
